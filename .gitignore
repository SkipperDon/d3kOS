# Secrets & credentials — NEVER commit these
api-keys.json
cloud-credentials.json
.env
.env.*
**/config/api-keys.json
**/config/cloud-credentials.json
**/config/*.env
telemetry-buffer.db

# Pi SSH keys
*.pem
id_d3kos
id_rsa

# Python
__pycache__/
*.pyc
.venv/

# Node
node_modules/

# macOS
.DS_Store

# Windows metadata (WSL alternate data stream files)
*:Zone.Identifier

# Office documents — use Markdown only
*.docx
*.odt
*.xlsx

# Session backup files and scratch
*.bak-*
ssh
